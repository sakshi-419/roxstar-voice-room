# Frontend Environment Variables Template
# When deployed to Vercel, set this to your deployed Token Server URL.
# During local development, leave blank to use the local Vite proxy (/health, /token, /dispatch).
VITE_TOKEN_SERVER_URL=https://your-token-server.onrender.com
